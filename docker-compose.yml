services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-neonmonkey}
      POSTGRES_USER: ${POSTGRES_USER:-neonmonkey}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-neonmonkey_dev_password}
    volumes:
      - neonmonkey-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 12

  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_HOST: postgres
      DATABASE_PORT: "5432"
      DATABASE_NAME: ${POSTGRES_DB:-neonmonkey}
      DATABASE_USER: ${POSTGRES_USER:-neonmonkey}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD:-neonmonkey_dev_password}
    ports:
      - "${APP_PORT:-8080}:8080"

volumes:
  neonmonkey-postgres:
