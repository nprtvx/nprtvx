services:
  - type: web
    name: neonmonkey
    runtime: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: neonmonkey-db
          property: connectionString
      - key: STATIC_DIR
        value: /app/static
      - key: NEONMONKEY_COOKIE_SECURE
        value: "true"

databases:
  - name: neonmonkey-db
    plan: free
    databaseName: neonmonkey
    user: neonmonkey
